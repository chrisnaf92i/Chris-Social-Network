import React, { Fragment } from 'react'

export default function CommentaryList(props) {
    const commentarys = props.commentaryList
    return (
        <Fragment>
            {/* {commentarys.map((test, id) => {
                <h1 key={id}>{test}</h1>
            })} */}
        </Fragment>
    )
}
